# def metadata_string(
#     add_metadata: bool, metadata_info: MappingProxyType[str, str]
# ) -> str | None:
#     """
#     Convert a read-only dictionary-like object (MappingProxyType) containing metadata information into a string.
#     """
#     if add_metadata:
#         return "\n".join(
