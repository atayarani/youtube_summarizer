{
  "packages": [
    "python310@latest",
    "poetry@latest"
  ],
  "shell": {
    "init_hook": [
      "poetry install && poetry shell"
    ],
    "scripts": {
      "test": [
        "poetry run pytest --cov=./ --cov-report xml -v"
      ]
    }
  }
}
